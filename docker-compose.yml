services:
  algomoster-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: algomoster-cyberflow
    ports:
      - "8080:80"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
